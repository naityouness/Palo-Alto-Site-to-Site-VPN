<vpn-config>
    <peer-ip>{{ peerIP }}</peer-ip>
    <pre-shared-key>{{ preSharedKey }}</pre-shared-key>
    <local-subnet>{{ localSubnet }}</local-subnet>
    <remote-subnet>{{ remoteSubnet }}</remote-subnet>
    <ike>
        <version>{{ ikeVersion }}</version>
        <!-- Add more IKE parameters if needed -->
    </ike>
    <!-- Add more configuration parameters as needed -->
</vpn-config>
